# Note: highly recommended having versions of each package, like so: Django==3.2.7

# Install Django
Django==3.2.8

# Required for Django timezones. This is from the django docs: https://docs.djangoproject.com/en/dev/topics/i18n/timezones/
pytz

# Postgres Database Adapter (requires 'python3-dev' and 'libpq-dev' packages in Ubuntu)
psycopg2

# Database config loader
dj-database-url
